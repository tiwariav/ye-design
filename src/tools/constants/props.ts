export const COMPONENT_SIZES = ["small", "large"] as const;
export const COMPONENT_SPACINGS = ["less", "extra"] as const;
export const COMPONENT_FLOAT = [
  "highest",
  "high",
  "medium",
  "low",
  "lowest",
] as const;
